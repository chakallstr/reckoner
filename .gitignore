# Dependencies
node_modules/

# Secrets / local config — NEVER commit these
.env
.env.*
*.local
secrets/
*.pem
*.key

# Build / output
dist/
build/
coverage/

# Local data / agent state
data/
*.db
*.sqlite
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Ruflo MCP scratch artifacts (never commit)
*.rvf
*.rvf.lock
ruvector.db
agentdb.rvf

# Private build plans / design docs — kept local, NOT in the public skeleton
# (they reference the private downstream agent + owner identities)
docs/superpowers/
